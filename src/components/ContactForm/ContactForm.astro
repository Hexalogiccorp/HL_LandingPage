<section id="contactanos" class="contact-section">
  <div class="decorative-lines">
    <svg class="decorative-svg" viewBox="0 0 1000 1000" fill="none">
      <path
        d="M0 500 Q250 400 500 500 T1000 500"
        stroke="currentColor"
        stroke-width="2"></path>
      <path
        d="M0 600 Q250 500 500 600 T1000 600"
        stroke="currentColor"
        stroke-width="2"></path>
      <path
        d="M0 400 Q250 300 500 400 T1000 400"
        stroke="currentColor"
        stroke-width="2"></path>
    </svg>
  </div>

  <div class="container">
    <div class="contact-info">
      <h2>Comunicate con nosotros</h2>
      <p>
        Puedes ponerte en contacto con nosotros a través de los siguientes
        medios. Ya sea para colaborar, resolver dudas o explorar nuevas
        oportunidades, siempre es un buen momento para construir algo increíble
        juntos.
      </p>
      <div class="contact-info-directions">
        <div class="svg-container">
          <div class="svg-background">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              width="24"
              height="24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-label="GPS"
            >
              <path
                d="M12 2C8.686 2 6 4.686 6 8c0 5.25 6 12 6 12s6-6.75 6-12c0-3.314-2.686-6-6-6z"
              ></path>
              <circle cx="12" cy="8" r="2.5" fill="none"></circle>
            </svg>
          </div>
              <div class="contact-Text">
              <h4>Dirección</h4>
              <p>Limón, Costa Rica</p>
            </div>
        </div>
        <div class="svg-container">
          <div class="svg-background">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              width="24"
              height="24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-label="Email"
            >
              <rect x="3" y="6" width="18" height="12" rx="2" ry="2"></rect>
              <path d="M3 8.5l9 6 9-6"></path>
            </svg>
          </div>
           <div class="contact-Text">
              <h4>Correo</h4>
              <p>hexalogiccorp@gmail.com</p>
            </div>
            
        </div>
        <div class="svg-container">
          <div class="svg-background">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              width="24"
              height="24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-label="Teléfono"
            >
              <path
                d="M22 16.92v3a2 2 0 0 1-2.18 2 19.86 19.86 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6A19.86 19.86 0 0 1 3.09 4.18 2 2 0 0 1 5 2h3a2 2 0 0 1 2 1.72c.12.86.38 1.69.76 2.47a2 2 0 0 1-.45 2.11L9.91 9.91a15.05 15.05 0 0 0 6 6l1.61-1.61a2 2 0 0 1 2.11-.45c.78.38 1.61.64 2.47.76A2 2 0 0 1 22 16.92z"
              ></path>
            </svg>
          </div>
           <div class="contact-Text">
              <h4>Número de teléfono</h4>
              <p>+(506) 6483-2448 | 8552-1881</p>
            </div>
        </div>
      </div>
    </div>
    <div class="form-wrapper">
      <h2 class="form-title">Contáctanos</h2>
      <form class="contact-form" netlify>
        <div class="form-group">
          <label for="fullname" class="form-label">Nombre completo</label>
          <input
            id="fullname"
            type="text"
            placeholder="Nombre"
            class="form-input"
          />
        </div>
        <div class="form-group">
          <label for="email" class="form-label">Email</label>
          <input
            id="email"
            type="email"
            placeholder="Correo"
            class="form-input"
          />
        </div>

        <div class="form-group">
          <label for="company" class="form-label">Nombre de la empresa</label>
          <input
            id="company"
            type="text"
            placeholder="Nombre de la empresa"
            class="form-input"
          />
        </div>

        <div class="form-group">
          <label for="information" class="form-label"
            >Me gustaría información sobre:</label
          >
          <select id="information" class="form-select">
            <option value="">Me gustaría saber más sobre...</option>
            <option value="landing">Landing Page</option>
            <option value="website">Página Web</option>
            <option value="system">Sistema Web</option>
            <option value="other">Otro</option>
          </select>
        </div>

        <div class="form-group">
          <label for="message" class="form-label">Mensaje</label>
          <textarea
            id="message"
            placeholder="Mensaje"
            class="form-textarea"
            rows="5"></textarea>
        </div>

        <button type="submit" class="submit-button"> Enviar Mensaje </button>
      </form>
    </div>
  </div>
</section>

<style>
  svg {
    color: white;
  }
  .svg-container {
    display: flex;
    gap: 1rem;
    align-items: center;
  }
  .contact-info-directions {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .svg-background {
    background-color: var(--emerald-600);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 2.2rem;
    width: 2.2rem;
    border-radius: 100%;
  }
  .contact-info {
    max-width: 30rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .contact-section {
    padding: 5rem 0;
    position: relative;
    overflow: hidden;
    background-color: var(--emerald-50);
  }

  .decorative-lines {
    position: absolute;
    inset: 0;
    opacity: 0.1;
    z-index: 1;
  }

  .decorative-svg {
    width: 100%;
    height: 100%;
    color: var(--emerald-400);
  }

  .container {
    max-width: 70rem;
    gap: 2rem;
    margin: 0 auto;
    position: relative;
    z-index: 10;
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
  }

  .form-wrapper {
    width: 30rem;
    height: 45rem;
    background-color: var(--white);
    border: 1px solid var(--gray-200);
    border-radius: 0.5rem;
    box-shadow:
      0 10px 15px -3px rgba(0, 0, 0, 0.1),
      0 4px 6px -2px rgba(0, 0, 0, 0.05);
    padding: 2rem;
    border-top: 4px solid var(--emerald-600);
  }

  .form-title {
    font-size: 1.875rem;
    font-weight: 700;
    text-align: center;
    color: var(--emerald-700);
    margin-bottom: 2rem;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
  }

  .form-label {
    color: var(--gray-700);
    font-weight: 500;
    margin-bottom: 0.25rem;
    font-size: 0.875rem;
  }

  .form-input,
  .form-select,
  .form-textarea {
    margin-top: 0.25rem;
    background-color: var(--white);
    border: 1px solid var(--gray-300);
    border-radius: 0.375rem;
    padding: 0.75rem;
    color: var(--gray-900);
    font-size: 1rem;
    transition: all 0.2s ease;
  }

  .form-input:focus,
  .form-select:focus,
  .form-textarea:focus {
    outline: none;
    border-color: var(--emerald-500);
    box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
  }

  .form-input::placeholder,
  .form-textarea::placeholder {
    color: var(--gray-400);
  }

  .form-textarea {
    min-height: 120px;
    resize: vertical;
  }

  .form-select {
    cursor: pointer;
    background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='m6 8 4 4 4-4'/%3e%3c/svg%3e");
    background-position: right 0.5rem center;
    background-repeat: no-repeat;
    background-size: 1.5em 1.5em;
    padding-right: 2.5rem;
    appearance: none;
  }

  .submit-button {
    width: 100%;
    background-color: var(--emerald-600);
    color: var(--white);
    padding: 0.75rem;
    border: none;
    border-radius: 0.375rem;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    box-shadow: 0 4px 6px -1px rgba(16, 185, 129, 0.2);
    transition: all 0.2s ease;
  }

  .submit-button:hover {
    background-color: var(--emerald-700);
    transform: translateY(-1px);
    box-shadow: 0 6px 10px -1px rgba(16, 185, 129, 0.3);
  }

  .submit-button:active {
    transform: translateY(0);
  }

  /* Responsive Design */
  @media (min-width: 768px) {
    .form-row {
      grid-template-columns: repeat(2, 1fr);
    }

    .form-wrapper {
      padding: 2.5rem;
    }
  }

  @media (max-width: 767px) {
    .contact-section {
      padding: 3rem 1rem;
    }

    .form-title {
      font-size: 1.5rem;
    }

    .form-wrapper {
      padding: 1.5rem;
    }

    .contact-form {
      gap: 1.25rem;
    }

    .form-row {
      gap: 1.25rem;
    }
  }

  @media (max-width: 480px) {
    .form-wrapper {
      padding: 1rem;
    }

    .form-title {
      font-size: 1.25rem;
      margin-bottom: 1.5rem;
    }

    .form-input,
    .form-select,
    .form-textarea {
      padding: 0.625rem;
    }
  }
</style>
