

<section id="contactanos" class="contact-section">
  <div class="decorative-lines">
    <svg class="decorative-svg" viewBox="0 0 1000 1000" fill="none">
      <path
        d="M0 500 Q250 400 500 500 T1000 500"
        stroke="currentColor"
        stroke-width="2"
      />
      <path
        d="M0 600 Q250 500 500 600 T1000 600"
        stroke="currentColor"
        stroke-width="2"
      />
      <path
        d="M0 400 Q250 300 500 400 T1000 400"
        stroke="currentColor"
        stroke-width="2"
      />
    </svg>
  </div>

  <div class="container">
    <div class="form-wrapper">
      <h2 class="form-title">Contáctanos</h2>
      <form class="contact-form">
        <div class="form-row">
          <div class="form-group">
            <label for="firstName" class="form-label">Nombre</label>
            <input
              id="firstName"
              type="text"
              placeholder="Nombre"
              class="form-input"
            />
          </div>
          <div class="form-group">
            <label for="lastName" class="form-label">Apellido</label>
            <input
              id="lastName"
              type="text"
              placeholder="Apellido"
              class="form-input"
            />
          </div>
        </div>

        <div class="form-group">
          <label for="email" class="form-label">Email</label>
          <input
            id="email"
            type="email"
            placeholder="Correo"
            class="form-input"
          />
        </div>

        <div class="form-group">
          <label for="company" class="form-label">Nombre de la empresa</label>
          <input
            id="company"
            type="text"
            placeholder="Nombre de la empresa"
            class="form-input"
          />
        </div>

        <div class="form-group">
          <label for="information" class="form-label">Me gustaría información sobre:</label>
          <select id="information" class="form-select">
            <option value="">Me gustaría saber más sobre...</option>
            <option value="landing">Landing Page</option>
            <option value="website">Página Web</option>
            <option value="system">Sistema Web</option>
            <option value="other">Otro</option>
          </select>
        </div>

        <div class="form-group">
          <label for="message" class="form-label">Mensaje</label>
          <textarea
            id="message"
            placeholder="Mensaje"
            class="form-textarea"
            rows="5"
          ></textarea>
        </div>

        <button type="submit" class="submit-button">
          Enviar Mensaje
        </button>
      </form>
    </div>
  </div>
</section>

<style>
  .contact-section {
    padding: 5rem 0;
    position: relative;
    overflow: hidden;
    background-color: var(--gray-50);
  }

  .decorative-lines {
    position: absolute;
    inset: 0;
    opacity: 0.1;
    z-index: 1;
  }

  .decorative-svg {
    width: 100%;
    height: 100%;
    color: var(--emerald-400);
  }

  .container {
    max-width: 64rem;
    margin: 0 auto;
    position: relative;
    z-index: 10;
  }

  .form-wrapper {
    background-color: var(--white);
    border: 1px solid var(--gray-200);
    border-radius: 0.5rem;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    padding: 2rem;
    border-top: 4px solid var(--emerald-600);
  }

  .form-title {
    font-size: 1.875rem;
    font-weight: 700;
    text-align: center;
    color: var(--emerald-700);
    margin-bottom: 2rem;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
  }

  .form-label {
    color: var(--gray-700);
    font-weight: 500;
    margin-bottom: 0.25rem;
    font-size: 0.875rem;
  }

  .form-input,
  .form-select,
  .form-textarea {
    margin-top: 0.25rem;
    background-color: var(--white);
    border: 1px solid var(--gray-300);
    border-radius: 0.375rem;
    padding: 0.75rem;
    color: var(--gray-900);
    font-size: 1rem;
    transition: all 0.2s ease;
  }

  .form-input:focus,
  .form-select:focus,
  .form-textarea:focus {
    outline: none;
    border-color: var(--emerald-500);
    box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
  }

  .form-input::placeholder,
  .form-textarea::placeholder {
    color: var(--gray-400);
  }

  .form-textarea {
    min-height: 120px;
    resize: vertical;
  }

  .form-select {
    cursor: pointer;
    background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='m6 8 4 4 4-4'/%3e%3c/svg%3e");
    background-position: right 0.5rem center;
    background-repeat: no-repeat;
    background-size: 1.5em 1.5em;
    padding-right: 2.5rem;
    appearance: none;
  }

  .submit-button {
    width: 100%;
    background-color: var(--emerald-600);
    color: var(--white);
    padding: 0.75rem;
    border: none;
    border-radius: 0.375rem;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    box-shadow: 0 4px 6px -1px rgba(16, 185, 129, 0.2);
    transition: all 0.2s ease;
  }

  .submit-button:hover {
    background-color: var(--emerald-700);
    transform: translateY(-1px);
    box-shadow: 0 6px 10px -1px rgba(16, 185, 129, 0.3);
  }

  .submit-button:active {
    transform: translateY(0);
  }

  /* Responsive Design */
  @media (min-width: 768px) {
    .form-row {
      grid-template-columns: repeat(2, 1fr);
    }

    .form-wrapper {
      padding: 2.5rem;
    }
  }

  @media (max-width: 767px) {
    .contact-section {
      padding: 3rem 1rem;
    }

    .form-title {
      font-size: 1.5rem;
    }

    .form-wrapper {
      padding: 1.5rem;
    }

    .contact-form {
      gap: 1.25rem;
    }

    .form-row {
      gap: 1.25rem;
    }
  }

  @media (max-width: 480px) {
    .form-wrapper {
      padding: 1rem;
    }

    .form-title {
      font-size: 1.25rem;
      margin-bottom: 1.5rem;
    }

    .form-input,
    .form-select,
    .form-textarea {
      padding: 0.625rem;
    }
  }
</style>
